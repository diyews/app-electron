import { AwayBottomDirective } from './away-bottom.directive';

describe('AwayBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new AwayBottomDirective();
    expect(directive).toBeTruthy();
  });
});
